import { ModelpNamePipe } from './modelp-name.pipe';

describe('ModelpNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ModelpNamePipe();
    expect(pipe).toBeTruthy();
  });
});
